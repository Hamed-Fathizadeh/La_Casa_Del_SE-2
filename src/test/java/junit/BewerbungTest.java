package junit;

public class BewerbungTest {
    /*
    private int bewerbungID = 1;
    private Date datum = null;
    private String description = "Hi ich bin's";
    private byte[] lebenslauf = null;
    private int status = 1;
    private int studentID = 1;
    private int anzeigeID;
     Bewerbung test;

    {
        test = new Bewerbung(bewerbungID,datum,description,lebenslauf,status,studentID,anzeigeID);
    }

    @Test
    // Bewerbung test = new Bewerbung(bewerbungID,datum,description,lebenslauf,status,studentID,anzeigeID);
    public void setTest() {
        test.setBewerbungID(bewerbungID);
        test.setDatum(datum);
        test.setDescription(description);
        test.setLebenslauf(lebenslauf);
        test.setStatus(status);
        test.setStudentID(studentID);
        test.setAnzeigeID(anzeigeID);
        Assert.assertEquals(bewerbungID,test.getBewerbungID());
        Assert.assertEquals(datum,test.getDatum());
        Assert.assertEquals(description,test.getDescription());
        Assert.assertEquals(lebenslauf,test.getLebenslauf());
        Assert.assertEquals(status,test.getStatus());
        Assert.assertEquals(studentID,test.getStudentID());
    }
    @Test
    public void getTest() {
        Bewerbung test2 = new Bewerbung(bewerbungID,datum,description,lebenslauf,status,studentID,anzeigeID);
        Assert.assertEquals(test.getBewerbungID(),test2.getBewerbungID());
        Assert.assertEquals(test.getDatum(),test2.getDatum());
        Assert.assertEquals(test.getDescription(),test2.getDescription());
        Assert.assertEquals(test.getLebenslauf(),test2.getLebenslauf());
        Assert.assertEquals(test.getStatus(),test2.getStatus());
        Assert.assertEquals(test.getStudentID(),test2.getStudentID());
    }

     */
}
